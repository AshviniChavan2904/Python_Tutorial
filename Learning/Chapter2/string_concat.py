#String is a collection of characters enclosed in a single/double quotes
first_name="Ashvini"
last_name="Chavan"
full_name=first_name+" "+last_name
print(full_name)
# print(full_name+3) We can't merge string with any number using +
# If we need to do so we need to convert the type to str(3)
# We can use * with string it prints that string that number of times
print(first_name*2)