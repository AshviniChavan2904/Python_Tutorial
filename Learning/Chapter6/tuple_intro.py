# Tuple is immutable data type hence we cannot use any method that modifies the data
# Tuple performance is better than list
# The methods supported by tuple are count, index and can use len function
# Can store mixed data types
days=('mon','tues','wed','thurs','fri','sat','sun')